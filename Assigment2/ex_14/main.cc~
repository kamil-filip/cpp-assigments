#include <iostream>

using namespace std;

int main()
{
	string line="";

	cout << 
			(
				getline(cin,line).eof() ?
					"incomplete line"
				:
					"complete line"
			) << "\n";
			
	
	//gets should never be used, as you run the serious risk of buffer overruns



	return 0;
}
